<% provide :title, t("admin.subject.title") %>
<div class="row">
  <h2 class="h2_title">
    <%= t "admin.subject.title" %>
  </h2>
  <%= render @subjects %>
  <div class="col-md-offset-3 col-md-7">
    <%= paginate @subjects %>
  </div>
</div>
